from setuptools import setup

setup(name='wchowdhu_distributions_v2',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['wchowdhu_distributions_v2'],
      zip_safe=False)
